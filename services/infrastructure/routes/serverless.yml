service: routes

custom:
  stage: ${opt:stage, self:provider.stage}

provider:
  name: aws
  stage: dev
  region: eu-central-1

resources:
  Resources:
    BasicZone:
      Type: AWS::Route53::HostedZone
      Properties:
        Name: testdev.dataplattform.knowit.no
    #BasicRecords:
      #Type: 'AWS::Route53::RecordSetGroup'
      #Properties:
        #HostedZoneId:
          #Ref: BasicZone
        #RecordSets:
          #- Name: testdev.dataplattform.knowit.no
            #Type: A
            #AliasTarget:
              #HostedZoneId: !Ref BasicZone
              #DNSName: doc.testdev.dataplattform.knowit.no

Outputs:
  routesZoneId:
    Value: !Ref BasicZone
    Export:
      Name: ${self:custom.stage}-routes-zoneid
